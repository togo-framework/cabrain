# cabrain

A togo plugin (mini-app) for [togo](https://github.com/togo-framework/togo).

```bash
togo install <owner>/cabrain
```

Exposes `GET /api/cabrain/ping` and injects `web/app/cabrain/page.tsx`.
