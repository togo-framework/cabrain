[Unit]
Description=Re-index registered repos every 30 minutes (sha-gated, so mostly a no-op)

[Timer]
OnBootSec=10min
OnUnitActiveSec=30min
AccuracySec=1min
RandomizedDelaySec=2min
Unit=cabrain-code-index.service

[Install]
WantedBy=timers.target
