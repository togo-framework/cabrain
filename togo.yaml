name: cabrain
module: github.com/togo-framework/cabrain

database:
  driver: pgx
  url: ${DATABASE_URL}

api:
  graphql: /graphql
  rest: /api
  docs: /docs

auth:
  provider: supabase

frontend:
  dir: web

plugins: []
