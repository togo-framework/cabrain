# togo-postgres — batteries-included Postgres (pg_duckdb, pg_search/BM25, pgvector,
# pg_cron, pg_partman) + Supabase-role compat. NATS JetStream is the MQ.
# Image: ghcr.io/togo-framework/db (github.com/togo-framework/db)
services:
  postgres:
    image: ghcr.io/togo-framework/db:latest
    container_name: cabrain-pg
    command:
      - postgres
      - -c
      - shared_preload_libraries=pg_cron,pg_duckdb,pg_search
      - -c
      - cron.database_name=cabrain
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cabrain
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  nats:
    image: nats:2          # NATS JetStream — message queue
    container_name: cabrain-nats
    command: ["-js"]
    ports:
      - "4222:4222"
      - "8222:8222"

  gotrue:
    image: supabase/gotrue:v2.151.0
    container_name: cabrain-gotrue
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "9999:9999"
    environment:
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: "9999"
      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_DATABASE_URL: "postgres://supabase_auth_admin:postgres@postgres:5432/cabrain?search_path=auth"
      GOTRUE_SITE_URL: http://localhost:3000
      GOTRUE_JWT_SECRET: "super-secret-jwt-token-replace-in-production"
      GOTRUE_JWT_EXP: "3600"
      API_EXTERNAL_URL: http://localhost:9999

  storage:
    image: supabase/storage-api:v1.11.6
    container_name: cabrain-storage
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "5000:5000"
    environment:
      ANON_KEY: "your-anon-key"
      SERVICE_KEY: "your-service-key"
      POSTGREST_URL: http://localhost:3000
      PGRST_JWT_SECRET: "super-secret-jwt-token-replace-in-production"
      DATABASE_URL: "postgres://supabase_storage_admin:postgres@postgres:5432/cabrain"
      FILE_SIZE_LIMIT: "52428800"
      STORAGE_BACKEND: file
      FILE_STORAGE_BACKEND_PATH: /var/lib/storage
      TENANT_ID: stub
      REGION: local
      GLOBAL_S3_BUCKET: stub
    volumes:
      - storagedata:/var/lib/storage

volumes:
  pgdata:
  storagedata:
